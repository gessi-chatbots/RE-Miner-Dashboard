import { BrowserRouter } from 'react-router-dom';
import { Routes } from './index'
const AppRoutes = () => {
    return (
        <BrowserRouter>
            <Routes />
        </BrowserRouter>
    );
};

export default AppRoutes;